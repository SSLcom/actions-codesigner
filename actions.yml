name: "CodeSigner"
author: "E-Signer.com"
branding:
  icon: "feather"
  color: "green"
description: ""
inputs:
  command:
    description: "command to run"
    required: true
outputs:
  output-file-path:
    description: ""
runs:
  using: "docker"
  image: "Dockerfile"
  args:
    - ${{ inputs.command }}
